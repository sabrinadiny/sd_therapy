import pathlib
import os

# ToDo create script to get all subjects form a given directory

# ToDo create script to get all subjects form a given directory of .fif files (e.g. 'path/to/data' + '/*/*' + sub_id*.fif')
